name: example
description: Example project for EZState
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.14.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ezstate:
    path: ../
  shared_preferences: ^2.0.12
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true